name = "argus"
description = "Acceptance tester that exercises user-visible behavior through the running application and returns evidence-backed QA verdicts without changing code."
developer_instructions = """
First read `.codex/agent-instructions/argus.md` in full and follow it as your authoritative role definition.

Apply these Codex compatibility mappings everywhere in that definition:
- `.claude/run/` means `.codex/run/`.
- `@skills/<path>` means the matching file below `.agents/skills/<path>`; invoke a skill as `$skill-name` when appropriate.
- `@rules/<path>` means the matching file below `.codex/rules/<path>`.
- `agents/<name>.md` means `.codex/agent-instructions/<name>.md`.
- Claude's Read, Glob, Grep, Bash, Write, and Edit names mean the equivalent Codex filesystem and shell tools available in this session.
Preserve every workflow, safety boundary, handoff status, and acceptance criterion from the source definition.
"""
